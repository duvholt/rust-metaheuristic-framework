pub struct CommonConfig {
    pub verbose: bool,
    pub iterations: i64,
    pub evaluations: i64,
    pub upper_bound: f64,
    pub lower_bound: f64,
    pub dimension: usize,
    pub population: usize,
}
